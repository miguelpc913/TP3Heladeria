<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tp4heladeria">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TP4Heladeria">
        <activity android:name=".HistorialDePedidosActivity"></activity>
        <activity android:name=".AddRepartidorActivity" />
        <activity android:name=".LoginManagerActivity" />
        <activity android:name=".ThankYouActivity" />
        <activity android:name=".SplashScreenActivity" />
        <activity android:name=".CheckoutActivity" />
        <activity android:name=".LoginEmpleadoActivity" />
        <activity
            android:name=".GerenteActivity"
            android:label="@string/title_activity_gerente" />
        <activity
            android:name=".RepartidorActivity"
            android:label="@string/title_activity_repartidor" />
        <activity
            android:name=".ClientActivity"
            android:label="@string/title_activity_client" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>